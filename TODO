General
=======
Establish a system to NOT set a variable via the source command:
eg. . %p/sbin/fink-buildenv-cmake.sh --noset=CMAKE_INSTALL_PREFIX would 
neither set $CMAKE_INSTALL_PREFIX nor add it to $FINK_CMAKE_ARGS.

Script specific
===============
